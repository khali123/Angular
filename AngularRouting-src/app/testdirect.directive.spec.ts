import { TestdirectDirective } from './testdirect.directive';

describe('TestdirectDirective', () => {
  it('should create an instance', () => {
    const directive = new TestdirectDirective();
    expect(directive).toBeTruthy();
  });
});
